build_settings:
  verbose: false
  ignore:
    - "vendor"
    - "tests"
    - "app"
    - "web"
    - "bin"

setup:
  composer:
    action: "install"
    prefer_dist: false

failure:
  email:
    committer: true
